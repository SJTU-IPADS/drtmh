#ifndef SIMPLE_LOG
#define SIMPLE_LOG


// This is a simple wrapper to help manage debug infors

#include "mapped_log.h"


namespace simple_log {

  class SimpleLogger {

    // assert the results, if the assertion fails, print the message and exit
    void inline debug_assert(bool expression, xxx) {

    }
  };
};

#endif
